# Node modules
node_modules/
backend/node_modules/
frontend/node_modules/

# Environment variables
.env
.env.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
# We can include frontend/dist if we want to serve it statically from backend on hosting that doesn't build it.
# Actually, standard practice is to ignore dist and build on the server, but since this is going to be deployed directly, sometimes dist is ignored.
# I will not ignore frontend/dist, just in case the user wants to deploy without a build step. But wait, standard Vercel/Render will run `npm run build`. Let's ignore it to keep repo clean.
frontend/dist/
dist/
build/

# Mac OS
.DS_Store
